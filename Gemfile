source "https://rubygems.org"

# Fastlane fallback build path (alternative to EAS). Install with `bundle install`
# and run lanes via `bundle exec fastlane ios build` / `android build`.
gem "fastlane"
